# This project is deliberately empty.

The source files for this project are produced using the AWS .NET SDK generator, through `telemetry\csharp\TelemetryClient.proj`.
They are not committed to the repo.
